// The test entry point. Ghost resolves imports against the directory of the
// file it was handed, so the suite is launched from the project root:
//
//   ghost test.gs
//
import "tests/core" as core
